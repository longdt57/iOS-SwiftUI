name: iOS starter workflow

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.1

      - name: Install Bundler
        run: gem install bundler

      - name: Install CocoaPods
        run: bundle install && bundle exec pod install --repo-update

      - name: Select Xcode version
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Build and Test
        run: xcodebuild clean test -workspace GitUser.xcworkspace -scheme GitUser -destination 'platform=iOS Simulator,name=iPhone 15,OS=latest' CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO
